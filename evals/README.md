# 行为评测

`cases/` 保存规则要解决的真实失败模式。每个用例包含输入、失败特征和合格结果，不要求唯一实现。

评测时使用 [评分标准](rubric.md)，重点检查：

- 输出是否直接完成目标；
- workflow 是否在公开入口可见；
- 名称是否匹配领域动作和状态所有者；
- 抽象层是否具有真实载荷；
- 正确性和安全边界是否完整。

新增规则前应先新增一个能复现问题的用例。

